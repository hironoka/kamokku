.panel.panel-default
  .panel-body
    %span= tweet.user.email
    %span= tweet.user.name
    %span= tweet.created_at.strftime('%Y/%m/%d %H:%M:%S')
  - if tweet.title?
    .panel-heading
      %span= tweet.title
  .panel-body
    %h5= tweet.tweet
